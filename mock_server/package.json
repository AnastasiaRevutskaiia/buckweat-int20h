{
  "name": "mock_server",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "json-server --watch db.json --port ${PORT:-5000} -H ${HEROKU_URL:-localhost} --static ../client/build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.16.3"
  }
}
